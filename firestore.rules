rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Everyone authenticated can read and write all data
    // (Simple rules for free tier - no paid Cloud Functions needed!)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
